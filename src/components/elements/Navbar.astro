---
import BtnLink from "../shared/BtnLink.astro";
import Container from "../shared/Container.astro";
import Navitem from "../shared/Navitem.astro";
import Logo from "../shared/Logo.astro";

// Détecter si on est sur la page de contact ou blog
const isContactPage = Astro.url.pathname === '/contact';
const isBlogPage = Astro.url.pathname === '/blog';

const navItems = [
    {
        href: isContactPage || isBlogPage ? "/" : "#",
        text: "Home"
    },
    {
        href: isContactPage || isBlogPage ? "/#services" : "#services",
        text: "Services"
    },
    /*{
        href: "/blog",
        text: "Blogs"
    },*/
    {
        href: "/contact",
        text: "Contact"
    }
]
---

<header class="absolute inset-x-0 top-0 z-50">
    <Container>
        <nav class="w-full flex items-center justify-between gap-6 relative pt-4">
            <div class="flex-shrink-0 z-10">
                <a href="/" class="block">
                    <Logo />
                </a>
            </div>

            <div data-nav-overlay aria-hidden="true" class="fixed hidden inset-0 lg:!hidden bg-box-bg bg-opacity-50 backdrop-filter backdrop-blur-xl"></div>
            <div data-navbar class="flex h-0 overflow-hidden lg:!h-auto lg:scale-y-100 duration-300 ease-linear flex-col gap-y-6 gap-x-4 lg:flex-row w-full lg:justify-between lg:items-center absolute lg:relative top-full lg:top-0 bg-body lg:bg-transparent border-x border-x-box-border lg:border-x-0">
                <ul class="border-t border-box-border lg:border-t-0 px-6 lg:px-0 pt-0 lg:pt-0 flex flex-col lg:flex-row gap-y-4 gap-x-14 text-lg text-heading-2 w-full lg:justify-center lg:items-center">
                    {navItems.map(item=>{
                        return <Navitem {...item}/>
                    })
                    }
                </ul>

                <div class="lg:min-w-max flex items-center sm:w-max w-full pb-6 lg:pb-0 border-b border-box-bg lg:border-0 px-6 lg:px-0">
                    <BtnLink text="Get in touch" href="/contact#calendly" className="flex justify-center w-full sm:w-max" variant="primary"/>
                </div>
            </div>

            <div class="min-w-max flex items-center gap-x-3">
                <button data-toggle-nav data-open-nav="false" class="lg:hidden lg:invisible outline-none w-7 h-auto flex flex-col relative">
                    <span id="line1" class="w-6 h-0.5 rounded-full bg-black dark:bg-white transition-all duration-300 ease-linear"></span> 
                    <span id="line2" class="w-6 origin-center mt-1 h-0.5 rounded-full bg-black dark:bg-white transition-all duration-300 ease-linear"></span> 
                    <span id="line3" class="w-6 mt-1 h-0.5 rounded-full bg-black dark:bg-white transition-all duration-300 ease-linear"></span>                         
                    <span class="sr-only">togglenav</span>
                </button>
            </div>
        </nav>
    </Container>
</header>

<script>
    import "../../scripts/theme-switcher.js";
</script>